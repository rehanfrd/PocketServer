name: pocket_server
description: A local server and file sharing app.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  file_picker: ^8.1.2
  permission_handler: ^11.3.0
  http: ^1.2.0

flutter:
  uses-material-design: true
